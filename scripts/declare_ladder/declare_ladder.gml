/// @function declare_ladder(pos_angle, top_radius, bottom_radius)
/// @param {real} pos_angle
/// @param {real} top_radius
/// @param {real} bottom_radius

var ladder = array_create(3);
ladder[0] = argument0;
ladder[1] = argument1;
ladder[2] = argument2;

return ladder;